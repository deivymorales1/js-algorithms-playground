/*
Enviar argumentos a funciones

function sumNumbers(...numbers){
    return numbers.reduce((total, num) => total + num, 0);
}

En este ejemplo, la funcion sumNumbers() puede recibir cualquier cantidad de argumentos y los trata como un array dentro de la funcion.

*/